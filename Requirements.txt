# Project Checklist: Simple Marketplace Web App

## Objective
Build a full-stack marketplace application where users can register, log in, list items for sale, and browse items.

## Technical Stack
- [ ] **Backend:** .NET Web API
- [ ] **Database:** Entity Framework Core with SQLite or SQL Server Express
- [ ] **Frontend:** Vanilla JavaScript, HTML, CSS
- [ ] **Authentication:** JWT (JSON Web Token) based

---

## Backend API Checklist

### User Management
- [X] **Endpoint:** `POST /api/users/register`
    - [X] Accepts `username`, `email`, `password`.
    - [X] Hashes the password securely (e.g., BCrypt).
    - [X] Creates a new user in the database.
    - [X] Returns the new user object (without password hash).
- [X] **Endpoint:** `POST /api/users/login`
    - [X] Accepts `email`, `password`.
    - [X] Verifies credentials against the database.
    - [X] Generates and returns a JWT on success.

### Product Management
- [X] **Endpoint:** `GET /api/products`
    - [X] Publicly accessible.
    - [X] Returns a JSON array of all products.
- [X] **Endpoint:** `GET /api/products/{id}`
    - [X] Publicly accessible.
    - [X] Returns a single product object by its ID.
- [X] **Endpoint:** `POST /api/products`
    - [X] Protected (requires JWT authentication).
    - [X] Accepts `name`, `description`, `price`.
    - [X] Associates the new product with the logged-in user.
    - [X] Returns the newly created product object.
- [X] **Endpoint:** `PUT /api/products/{id}`
    - [X] Protected (requires JWT authentication).
    - [X] Verifies that the logged-in user is the owner of the product.
    - [X] Updates the product's `name`, `description`, or `price`.
    - [X] Returns the updated product object.
- [X] **Endpoint:** `DELETE /api/products/{id}`
    - [X] Protected (requires JWT authentication).
    - [X] Verifies that the logged-in user is the owner of the product.
    - [X] Deletes the product from the database.
    - [X] Returns a 204 No Content success status.

---

## Frontend Checklist

### Core Pages
- [ ] **File:** `index.html` (Home Page)
    - [ ] Fetches and displays all products from the API.
    - [ ] Provides navigation links.
    - [ ] Navigation dynamically changes based on login status (Login/Register vs. My Products/Logout).
- [ ] **File:** `login.html`
    - [ ] Contains a login form (email, password).
    - [ ] On successful login, saves JWT to `localStorage`.
    - [ ] Redirects to `my-products.html` after login.
- [ ] **File:** `register.html`
    - [ ] Contains a registration form (username, email, password).
    - [ ] Redirects to `login.html` after successful registration.

### Product Interaction Pages
- [ ] **File:** `products.html` or similar (Product Detail Page)
    - [ ] Can be a single page that uses URL parameters (e.g., `products.html?id=123`).
    - [ ] Fetches and displays details for one specific product.
- [ ] **File:** `my-products.html` (User-specific page)
    - [ ] Accessible only if a JWT is present in `localStorage`.
    - [ ] Displays only the products owned by the logged-in user.
    - [ ] Includes a form to create a new product.
    - [ ] Each product in the list has an "Edit" button.
    - [ ] Each product in the list has a "Delete" button that works as expected.

---

## Final Deliverables
- [ ] **File:** `README.md`
    - [ ] Includes setup instructions for the database.
    - [ ] Includes instructions on how to run the backend.
    - [ ] Includes instructions on how to open/run the frontend.
- [ ] **Code Quality:** All code is clean, organized, and readable.
